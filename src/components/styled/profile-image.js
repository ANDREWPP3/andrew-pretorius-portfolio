import styled from "styled-components";
import { SIZE } from "../../const/sizes";

export const ProfileImage = styled.img`
    width : ${SIZE.COMPONENTS.LG};
    height : ${SIZE.COMPONENTS.LG};
    border-radius : ${SIZE.BORDER_RADIUS.CIRCLE}
`